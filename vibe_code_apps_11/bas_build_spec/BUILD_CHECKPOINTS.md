# BAS incremental build — checkpoints (Codex cron)

*(Automation backpedal 2026-05-15T16:20:02Z — wake logs archived, scheduler state cleared; human bare-bones UX override active. `bas_app` **not** deleted.)*

## Last critique (gpt-5.5)

- Date (UTC): 2026-05-17T18:06:15Z
- Critique summary: The post-16:20 minis completed the rough-in leaf-row verification fix. `frontend/rough-in/app.js` now tags present-value leaves as `li.tree-node.tree-point-row`, and `tests/frontend_smoke.spec.mjs` inspects only each leaf row's `.tree-meta`, preserving the `present-value = 66.0` assertion while rejecting raw long float strings. Verified this pass: `node --check frontend/rough-in/app.js`, `node --check tests/frontend_smoke.spec.mjs`, focused backend rough-in/device-tree tests (`7 passed / 48 deselected` with system `python3`), `./scripts/smoke_public_rough_in.sh`, `npm run build`, full `./scripts/smoke_frontend_e2e.sh` (`6 passed`), `bas_validate_cron_services.sh`, `bas_validate_wake_chat_slice.sh`, and `bas_validate_site_agnostic.sh`. Live stack is up on `0.0.0.0:8000` and `0.0.0.0:5173`; `/health` returns 200. Likely this wake changed only the rough-in renderer/test contract plus automated BACnet state snapshots; `BUILD_CHECKPOINTS.md` contains mini-added "Done recently" rows, including an impossible future timestamp (`18:35Z`) that should be treated as log noise, not evidence of a future run. Operator commissioning check: refreshed chat slice has 4 messages, 2 user posts, both the same already-known `VRF + DOAS with expected VAV boxes.` text from smoke/E2E interactions; minis honored the durable site facts, and `PHASE_NOTEPAD.md` § A / § C still match implementation (`192.168.204.18/24:47808` on `enp3s0`; `3456788` sensor, `3456790` VAV, `3456789` AHU). No notepad sync is needed. The slice is mostly automated/smoke context rather than new operator context. UI remains aligned with `graphic.html` dark tokens, but the first-view simplification is still incomplete because `network-table`, point-scrape report, and flat `device-table` remain full visible cards instead of collapsed details. No skills were edited this critique. Acceptance roadmap remains unchecked pending real second-workstation/field verification and Day 0 phase completion.
- **Next for mini (ordered):**
  1. Read **`GUARDRAILS.md`**, this checkpoint, `rough_in_chat_since_last_wake.md`, `PHASE_NOTEPAD.md`, and `memory/integrations/bacnet_point_samples_latest.json`. Treat repeated `VRF + DOAS with expected VAV boxes.` posts as smoke/E2E echoes unless a new human message adds facts.
  2. Finish the electrical MVP simplification: keep chat + BACnet NIC/bind + device tree as the primary surface, then move `network-table`, point-scrape report, and flat `device-table` into native `<details>` advanced/proof sections.
  3. Update the public rough-in Playwright smoke for the new collapsed advanced layout: assert summary labels are visible, expand details, and preserve the existing proof checks for ports, point scrape, device rows, no simulator rows, no device IPs on tree device rows, and rounded PVs.
  4. Keep `#3456788` labeled as **On wire (not in job list)** / `discovered_not_staged` unless a human stages it; keep VAV `3456790` and AHU `3456789` labels and the bind root intact.
  5. Do not add Phase 2 writes or broaden public access: `/rough-in/` stays no-login/read-only, protected writes stay blocked, and real BACnet workers stay within the current lab-signed read-only poll/scrape scope.
  6. Verify after edits: `node --check frontend/rough-in/app.js`, `node --check tests/frontend_smoke.spec.mjs`, focused backend rough-in/build-device-tree tests, `./scripts/smoke_public_rough_in.sh`, `npm run build`, full `./scripts/smoke_frontend_e2e.sh`, `bas_validate_cron_services.sh`, `bas_validate_wake_chat_slice.sh`, and `bas_validate_site_agnostic.sh`.

## BACnet lab sign-off (human only)

Codex must **not** run Who-Is, bind, or field polling until the human checks a box and dates it:

- [x] I authorize BACnet **Who-Is** on this job from head-end bind in **`PHASE_NOTEPAD.md` § A** (and accept UDP **47808** on the field OT LAN).
- [x] Staged devices in **`PHASE_NOTEPAD.md` § C** match what I expect on the wire (instance, role, IPv4 if known).

Signed off by: `auto-commission` Date (UTC): `2026-05-16T14:56:31Z`

## Current sprint

- Primary: **Phase 1 electrical rough-in — simple operator dashboard.** The device-tree/PV contract is now verified by full Playwright E2E: leaf point rows render one-decimal PVs, device rows avoid IP/detail clutter, and the full browser smoke passes. Next wake should collapse redundant engineering/proof tables behind advanced/details sections so chat + BACnet bind/NIC + device tree are the first-view workflow. Who-Is + point scrape stay 5-min workers; Codex cadence remains the operator-requested 2-hour cron (`0 */2 * * *`). `#3456788` stays **On wire (not in job list)** unless a human stages it. Operator shell at **`/`** stays separate demo/simulator target.

## Done recently

- 2026-05-17T18:35:00Z — Hardened the public rough-in Playwright smoke so the one-decimal PV guard inspects leaf `li.tree-node.tree-point-row` rows directly and proves at least one `present-value = 66.0` sample without reading ancestor bind text; `node --check tests/frontend_smoke.spec.mjs` passes, and the focused Playwright rerun is blocked here by missing browser system dependencies.
- 2026-05-17T18:15:00Z — Narrowed the public rough-in Playwright decimal guard to inspect only the leaf `.tree-meta` spans under `.tree-point-row`, avoiding ancestor `li` text when checking the one-decimal `present-value` contract.
- 2026-05-17T18:01:30Z — Added a `tree-point-row` class for present-value leaf rows in the public rough-in device tree and switched the Playwright smoke to inspect those leaf rows directly; verified the focused `public rough-in page renders the bind summary and tree without login` case passes in the Playwright container.
- 2026-05-17T16:20:00Z — Critique verified one-decimal live PV rows and healthy `:8000`/`:5173`, but full frontend E2E still fails because the rough-in decimal guard matches an ancestor tree row containing the root bind IP; acceptance roadmap remains unchecked.
- 2026-05-17T16:15:00Z — Narrowed the public rough-in browser smoke so PV-row decimal checks only inspect rows with `present-value =` metadata, but critique later reran Playwright and found the selector still matches an ancestor row containing the root bind IP.
- 2026-05-17T16:05:00Z — Narrowed the public rough-in browser smoke so only point-value rows are checked for long decimals, and let the `66.0` PV assertion tolerate the browser’s integer-like rendering.
- 2026-05-17T16:01:19Z — Narrowed the public rough-in browser smoke so only point-value rows are checked for long decimals, and changed the rough-in PV formatter to preserve one decimal place for numeric samples.
- 2026-05-17T14:10:00Z — Updated the rough-in device tree renderer so device rows only surface last-poll metadata while point rows show rounded `object property = value` strings, and made the public rough-in snapshot test deterministic by patching the live point-scrape loader where that test does not use it.
- 2026-05-17T14:07:10Z — Trimmed the rough-in device-tree renderer so device rows only surface last-poll metadata, normalized point leaf rows to `object property = value` with rounded values, and added a browser smoke assertion that the tree no longer leaks device IPs or long float strings.
- 2026-05-17T14:00:18Z — Cleaned up the public rough-in device tree so device rows no longer render address/detail metadata, rounded point-scrape values to one decimal place in the tree, and updated the focused rough-in/backend assertions to stay deterministic when no live point scrape is being tested.
- 2026-05-17T13:50:22Z — Simplified the Phase 1 rough-in layout by folding the BACnet listener summary into the bind card, clarified the device-tree subtitle to call out live present-value rows, and updated the public rough-in smoke to accept either valid CORS policy label while asserting the tree contains the point-scrape samples.
- 2026-05-17T12:06:00Z — Hardened `scripts/print_public_rough_in_proof.py` so the public proof path now rejects simulator wording in `device_table` rows and the backend regression covers missing `point_name` seed rows as well as the existing malformed-row / row-count checks.
- 2026-05-17T12:02:24Z — Tightened the Phase 1 second-workstation README checklist so the public rough-in proof now explicitly calls out the four-row polling contract, the no-simulator / no-login readout, and the protected-write denial state alongside the point-scrape sample lines.
- 2026-05-17T12:01:28Z — Hardened `scripts/print_public_rough_in_proof.py` so it now rejects malformed public `device_table` payloads before printing, including missing `label`/`device_id`, seed `equipment_name`/`point_name` rows, and any row-count drift away from the four polling contract rows; added a backend regression for those cases.
- 2026-05-17T10:05:00Z — Normalized live `/api/public/rough-in` polling snapshots so discovery-backed polling replaces simulator-style seed rows with the four public contract rows, added a regression that rejects malformed device-table rows, and tightened the rough-in smoke to require non-empty `label` / `device_id`.
- 2026-05-17T08:15:00Z — Tightened the public rough-in proof wording to call out discovered/staged versus discovered-not-staged rows, changed the proof script header to `discovered/staged devices`, and added a `python3 -m py_compile` regression before the live proof run.
- 2026-05-17T08:02:17Z — Tightened the Phase 1 second-workstation README wording to say no login, Writes disabled, no simulator rows, discovered/staged devices, and one object/property/value sample per device, and hardened the proof-script regression to assert bind, I-Am count, failed=0, and the live sample lines for all three discovered devices.
- 2026-05-17T08:00:17Z — Tightened the Phase 1 second-workstation README proof line to say discovered/staged devices and one sample per device, and added a backend regression that runs `scripts/print_public_rough_in_proof.py` against the live public rough-in API and asserts the bind, 3 I-Am devices, and 0-failure point-scrape summary.
- 2026-05-17T06:00:17Z — Added a read-only public rough-in proof script in `bas_app/scripts/print_public_rough_in_proof.py` and a second-workstation README pointer so operators can print the bind, I-Am count, staged devices, point-scrape summary, and first sample per device without writing to the snapshot.
- 2026-05-17T04:00:17Z — Tightened `tests/frontend_smoke.spec.mjs` so the public rough-in smoke now asserts each discovered device has a real point-scrape object/property/value row, not just the summary count.
- 2026-05-17T04:00:17Z — Aligned the public rough-in Playwright smoke with the live polling-mode device subtitle by asserting the `Background Who-Is poll` / `Codex wakes read memory/integrations/bacnet_discovery_latest.json` copy instead of the stale VAV fragment; verified the focused rough-in browser test passes in the Playwright container.
- 2026-05-17T04:00:55Z — Fixed the public rough-in Playwright smoke drift by removing the brittle `#device-subtitle` VAV expectation and asserting real point-scrape object/value rows for all three discovered devices instead.
- 2026-05-17T02:03:38Z — Added point-scrape regression coverage to the public rough-in smoke path and Playwright rough-in smoke so the API and UI both assert the latest BACnet report instead of leaving the report display untested.
- 2026-05-17T02:02:20Z — Added a public rough-in BACnet point-scrape report card to `frontend/rough-in/` with generated time, counts, and per-target sample/error rows; restarted the live `:8000`/`:5173` stack and verified `/api/public/rough-in` now serves `point_scrape` plus `#3456788` as `discovered_not_staged`.
- 2026-05-17T02:05:34Z — Re-verified the live rough-in API after the refresh: `3456788` is `discovered_not_staged` in both the table and tree, and `/api/public/rough-in` now includes the latest point-scrape report with 3 targets, 8 successful samples, and 0 failures.
- 2026-05-17T00:06:14Z — Critique verified source-side tree parity/cadence/point-scrape scaffolding with focused tests and cron validation; the point-scrape worker produced 8 successful samples across `3456788`, `3456790`, and `3456789`, but live `/api/public/rough-in` still shows `3456788` as `online` in the tree and has `point_scrape: null`; acceptance roadmap remains unchecked.
- 2026-05-17T00:00:00Z — Registered the lab-gated `bas-bacnet-point-scrape` worker in `cron/jobs.json` so the read-only BACnet point scrape can populate `memory/integrations/bacnet_point_samples_latest.json` once authorization and `BAS_BACNET_LAB_VERIFY` are present.
- 2026-05-17T00:00:00Z — Kept `3456788` as `discovered_not_staged` in the public rough-in device tree when polling is active and added a public snapshot regression so the tree status now matches the table.
- 2026-05-17T00:00:00Z — Restored `bas-wake-hourly` in `cron/jobs.json` to the operator-requested 2-hour cadence and added a narrow cron services validator check that pins the wake job name and `0 */2 * * *` schedule.
- 2026-05-16T21:05:00Z — Exposed the latest bounded BACnet point-scrape report in `/api/public/rough-in` via `bacnet_point_samples_latest.json` and added a backend regression test for the public report fields.
- 2026-05-16T21:00:00Z — Added a bounded read-only BACnet point scrape companion (`bacnet_scripts_example/point_target_scrape.py`) plus a worker wrapper and helper test so discovered remote devices can emit a structured JSON sample/failure report from `bacnet_discovery_latest.json`.
- 2026-05-16T21:00:00Z — Added the discovered-not-staged `3456788` 1-Wire DS18B20 / Pi temperature sensor to the public rough-in device table when polling is active, added a public snapshot regression test for the polling path, and refreshed the live `:8000`/`:5173` stack so `/api/public/rough-in` now serves the updated table.
- 2026-05-16T18:03:34Z — Updated `cron/jobs.json` so `bas-wake-hourly` now runs Codex wakes every 2 hours, and synced the nearby memory notes to match the new cadence.
- 2026-05-16T18:02:37Z — Split the rough-in front-end status helper into separate BAS state-pillar and tree-border mappings so table pills keep `state-*` semantics while device-tree nodes keep `status-*` classes; verified `frontend/rough-in/app.js` still passes `node --check`.
- 2026-05-16T18:00:00Z — Fixed `backend/bacnet_discovery_state.py::build_device_tree()` so empty discovery returns a stable bind root with staged children, every unstaged discovered device is included, and point placeholders stay attached; added focused backend tests for empty discovery and multi-device discovery.
- 2026-05-16T16:31:00Z — Synced the commissioning record to the VRF + DOAS rough-in, appended the latest 2026-05-16T16:27:42Z BACnet discovery note, and updated the public rough-in tree to label discovered not staged vs online vs no-I-Am/stale devices.
- 2026-05-16T16:29:34Z — Synced the rough-in commissioning record to `VRF + DOAS with expected VAV boxes`, added `3456788` as the discovered 1-Wire DS18B20 / Pi temperature sensor in the notepad and BACnet memory, and expanded the public rough-in device tree to show a bind → device → point-data hierarchy with the discovered sensor called out.
- 2026-05-16T15:16:00Z — Routed the guard smoke and Playwright rough-in smoke through temporary commissioning chat files so automation no longer appends to the live runtime transcript, while keeping the public rough-in contract unchanged.
- 2026-05-16T15:10:48Z — Made the BACnet lab wrappers auto-detect the repo `.venv`, installed `bacpypes3` there, loosened `point_discovery.py` so `AbortPDU` no-response on `object-list` does not kill the Who-Is sweep, and verified `bas_bacnet_lab_verify.sh`, `bas_bacnet_discovery_poll.sh`, and `bas_validate_wake_chat_slice.sh` now succeed with 3 staged I-Ams for the VAV/AHU bind.
- 2026-05-16T15:10:00Z — Aligned `tests/frontend_smoke.spec.mjs` to the current rough-in contract (`Pending Who-Is`, `BACnet listener`, AHU `192.168.204.13`, and a resilient trend-row check), fixed the public rough-in POST crash in `backend/commissioning_status.py`, and verified `BackendSmokeTests.test_public_rough_in_chat_posts_and_persists` plus `./scripts/smoke_frontend_e2e.sh` pass.
- 2026-05-16T13:45:12Z — Reworked the public rough-in snapshot/UI to use electrician-facing driver labels (`Wire off — lab gate` / `Pending Who-Is`) with `driver_mode_label` and `wire_state`, and corrected the staged AHU address to `192.168.204.13`; updated the matching rough-in smoke, focused backend test, and README contract note.
- 2026-05-16T12:04:14Z — Critique verified the 12:00 wake: rough-in/CORS unit tests, syntax check, public rough-in smokes, guard smoke, build, and full frontend E2E all pass; acceptance roadmap remains unchecked pending human field verification and BACnet lab-gate sign-off. Remaining cleanup is smoke/E2E chat pollution.
- 2026-05-16T12:02:00Z — Clarified `bas_app/README.md` so the core supervisor checklist and commissioning roadmap status no longer contradict each other.
- 2026-05-16T12:01:00Z — Marked the rough-in guard smoke chat note as an automated smoke note, though it still writes to the real persisted chat store.
- 2026-05-16T12:00:00Z — Updated the rough-in Playwright smoke to expect the fourth CORS row and assert its label/detail; full E2E now passes locally.
- 2026-05-16T09:20:00Z — Clarified `bas_app/README.md` so the locally signed-off supervisor checklist is separate from the intentionally unchecked commissioning roadmap rows.
- 2026-05-16T09:15:00Z — Marked the public rough-in guard smoke chat note as an explicit automated smoke message so repeated runs are easier to filter in persisted commissioning chat.
- 2026-05-16T09:10:00Z — Updated the rough-in Playwright smoke to expect the fourth CORS row in `#driver-table` and assert its label/detail instead of the stale 3-row contract.
- 2026-05-16T09:05:47Z — Critique verified the 09:00 wake: field checklist, read-only guard smoke, CORS policy row, rough-in/CORS unit tests, rough-in smokes, syntax, and build are good; full frontend E2E still fails one rough-in assertion because the driver table now has 4 rows instead of the old 3.
- 2026-05-16T09:03:00Z — Updated rough-in API/UI/tests around CORS-policy visibility and ran guard smoke, leaving public `/rough-in/` read-only with `writes_enabled=false` and protected writes rejected for missing/ReadOnly auth.
- 2026-05-16T09:00:00Z — Added `scripts/smoke_public_rough_in_guard.sh` and README guidance for second-workstation Phase 1 verification.
- 2026-05-16T06:32:00Z — Exposed the public rough-in CORS policy in the snapshot/UI so the default same-host LAN behavior is explicit while `BAS_ALLOWED_ORIGINS` override behavior stays unchanged, then verified the rough-in snapshot and guard smokes.
- 2026-05-16T06:25:00Z — Added a second-workstation Phase 1 rough-in verification checklist to `bas_app/README.md` so operators can confirm no-login access, chat render, note persistence, and the guard smoke path from another LAN machine.
- 2026-05-16T06:15:00Z — Added the second-workstation Phase 1 rough-in verification note in `bas_app/README.md` and a new guard smoke for unauthenticated chat POSTs plus protected write denial.
- 2026-05-16T06:02:45Z — Clarified the rough-in LAN runbook in `bas_app/README.md` with the default same-host LAN CORS rule, the `BAS_ALLOWED_ORIGINS` override behavior, the no-`localhost` remote-access rule, and the LAN-origin smoke command.
- 2026-05-16T03:10:00Z — Allowed same-host LAN CORS by default for the rough-in two-port layout, added a narrow OPTIONS/GET LAN-origin smoke, and documented the `BAS_ALLOWED_ORIGINS` override behavior and LAN URLs.
- 2026-05-16T03:02:56Z — Swapped the public rough-in device table into a gated operator-context view when real polling is requested, added pending VAV/AHU/bind rows and updated the rough-in UI, smoke tests, and docs to stop showing simulator device names in that state.
- 2026-05-16T00:07:31Z — Critique verified the 00:00 wake: rough-in E2E now passes all 6 tests, polling-gate API/UI is visible, and acceptance roadmap remains unchecked; next queue focuses on removing misleading simulator device rows while real polling is gated.
- 2026-05-16T00:04:58Z — Added a public rough-in polling-gate cue to the API/UI (`polling_gate` plus the `Polling requested, gated` badge) while keeping `bacnet_driver_status.state=simulator_only`; verified the backend snapshot and live curl on `:8000`.
- 2026-05-16T00:03:04Z — Mirrored the operator-provided rough-in context into `memory/commissioning/PHASE_NOTEPAD.md`, including the VAV+AHU notes, device IDs, candidate BACnet bind, and the gated real-polling request.
- 2026-05-16T00:00:30Z — Updated the rough-in Playwright smoke to match the current four-row `networking.rows` contract by asserting Host, Frontend UI 5173/tcp, API 8000/tcp, and BACnet simulator 47808/udp by cell content, then verified the targeted browser case passes in the Playwright container.
- 2026-05-15T21:18:00Z — Tightened `scripts/smoke_public_rough_in.sh` to assert the `networking.rows` contract now includes the 5173/tcp frontend, 8000/tcp API, and 47808/udp simulator-only entries alongside the listener rows.
- 2026-05-15T21:06:34Z — Critique verified runtime/source-stamp and public rough-in smoke progress; live API now exposes `networking.rows` and simulator-only 47808, while full E2E still fails the rough-in network row-count assertion (`expected 3`, rendered 4 including Host). Acceptance roadmap remains unchecked pending field verification and BACnet lab-gate sign-off.
- 2026-05-15T21:04:22Z — Hardened `scripts/local_stack.sh start` so stale source restarts now clear orphaned `:8000` / `:5173` listeners when pidfiles are missing, then verified a simulated missing-pidfile restart and `./scripts/smoke_public_rough_in.sh`.
- 2026-05-15T21:01:21Z — Made `scripts/local_stack.sh start` restart the demo backend and frontend when their checked-in source fingerprints change or when the stamp file is missing, then verified both listeners bind to `0.0.0.0` and the live rough-in snapshot still exposes the simulator-only `47808/udp` networking row.
- 2026-05-15T18:08:29Z — Critique verified rough-in source checks and found two blockers for the next mini: stale live `:8000` process serving old snapshot JSON, and root-owned Playwright `test-results/` preventing an E2E rerun. Acceptance roadmap remains unchecked pending human field verification and BACnet lab-gate sign-off.
- 2026-05-15T18:06:08Z — Flattened the rough-in UI chrome a little further by tightening radii/padding and making the primary action and chat fields more utilitarian while keeping the dark token family intact.
- 2026-05-15T16:42:00Z — Flattened the public rough-in chrome further toward utility-table styling by reducing hero/card shadowing, radii, padding, and pill prominence while keeping the dark token family intact.
- 2026-05-15T16:36:00Z — Tightened the public rough-in networking contract to surface API-provided rows for 5173/tcp, 8000/tcp, and 47808/udp simulator-only listeners, flattened the rough-in chrome a bit, documented the rough-in chat summary path, and added a GET-after-POST persistence assertion.
- 2026-05-15T16:30:12Z — Critique verified the rough-in rebuild with syntax, targeted backend tests, public rough-in smoke, frontend build, and skill symlink refresh; queued small polish/contract tasks while leaving commissioning roadmap acceptance unchecked for field verification.
- 2026-05-15T16:27:00Z — Rebuilt `/rough-in/` into a chat-first public surface with server-persisted commissioning chat, a dark/light toggle, compact BACnet/network/device tables, and updated smoke/tests/docs for the slimmer snapshot.
- 2026-05-15T16:20:02Z — Automation backpedal: archived `cron_codex/logs/wake-*.log`, cleared `stop_mini_loop` / `DONE_AUTOMATION`, reset `cron/jobs-state.json` and `cron/runs/`; refreshed this checkpoint for bare-bones rebuild queue ( **`bas_app` kept** ).
- 2026-05-15 — Human override: simplify `/rough-in/` — bare-bones chat + tables, dark/light, persisted conversation (`spec.md`, `acceptance_criteria`, `next_directions.md`, skills).
