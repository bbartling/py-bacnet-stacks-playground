# Frontend scaffold

The BAS workstation shell lives here as a static HTML/CSS slice for now.

Run it from this directory:

```bash
python3 -m http.server 5173 --bind 0.0.0.0
```

Then open `http://0.0.0.0:5173/`.
