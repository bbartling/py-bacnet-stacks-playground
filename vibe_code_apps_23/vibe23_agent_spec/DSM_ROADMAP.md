# DSM roadmap — residential

1. **DR demo** — July hot-afternoon precool / shed / recover (`vibe23 residential-dr`)
2. **Thermostat grid** — finite schedules within 69.5–74.5°F under illustrative TOU (`residential-grid`)
3. **Battery** — post-process / hierarchical dispatch on purchased-grid kW (`residential-battery-grid`)
4. **Providers** — `WeatherProvider` + `TariffProvider` fixtures; live APIs optional later
5. **Compute** — host.json + campaign wall vs aggregate process-seconds

Monetary ranking uses `ILLUSTRATIVE` tariffs for education only.
