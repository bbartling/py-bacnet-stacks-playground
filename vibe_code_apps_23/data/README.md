# Local data workspace

Run `vibe23 download --data-dir data` from `vibe_code_apps_23/`.

`raw/` contains the downloaded Dryad release and extracted telemetry. `processed/` contains generated inventories and calibration targets. Both remain local except placeholders/small intentional fixtures.
