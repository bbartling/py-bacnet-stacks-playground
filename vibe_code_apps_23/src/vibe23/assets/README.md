# Demo assets (packaged with vibe23)

Canonical copies for wheels / Streamlit Cloud live under:

- `src/vibe23/assets/residential_heat_pump_home.idf`
- `src/vibe23/assets/USA_CO_Golden-NREL.724666_TMY3.epw`

This `model/` folder keeps the same filenames as convenience mirrors for humans browsing the repo. Prefer the package assets path in code (`vibe23.residential.model`).
