# This tree was merged into `vibe_code_apps_8/`

The **easy-aso** BAS Lite stack (diy-bacnet + `bas_lite_api` + nginx frontend + Caddy) now lives in the **parent directory**:

**`../vibe_code_apps_8/`**

Use that folder for day-to-day work. This copy is kept only as a reference until you delete it.
