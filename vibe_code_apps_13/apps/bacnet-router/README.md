# bacnet-router

Phase 3 application placeholder. Add this crate only after Phase 2 passes.

Use B/IP network 100 plus MS/TP network 2001 with the pinned Rust stack's heterogeneous transport support. The management UI is downstream of a proven data plane.

