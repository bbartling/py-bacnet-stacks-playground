# serial-wire-test

Phase 1 application placeholder. Add this crate to the root Cargo workspace when implementation starts.

It owns two serial devices, alternates private raw envelopes and produces a JSON hardware report. See the phase document and local `AGENTS.md`.

