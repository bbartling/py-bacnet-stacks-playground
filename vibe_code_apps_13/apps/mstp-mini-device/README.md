# mstp-mini-device

Phase 2 application placeholder. Add this crate only after Phase 1 passes.

Port the upstream mini-device object model and services to `MstpTransport<TokioSerialPort>`. Do not bring over its BACnet/IP socket/network setup.

