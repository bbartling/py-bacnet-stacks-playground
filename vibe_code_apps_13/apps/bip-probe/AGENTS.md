# Phase 3 B/IP Probe Agent Rules

This probe runs in Linux network namespace `baclab` and tests routing; it is not part of the appliance. It must produce deterministic route-discovery, application-service, hop-count, rejection and broadcast evidence with packet captures/correlation.

