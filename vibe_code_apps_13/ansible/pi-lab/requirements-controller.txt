# Controller venv pins for Ubuntu tower (bensbench).
# Install: python3 -m venv .venv && .venv/bin/pip install -r requirements-controller.txt
ansible-core==2.17.9
ansible-lint==24.10.0
yamllint==1.35.1
# Required for ansible --ask-pass with password_mechanism=sshpass on this pin.
# System package alternative: sudo apt install sshpass
