# UI Repair Prompt

Treat the Sketchbox UI as changed. Perform read-only exploration, capture redacted artifacts, compare the UI fingerprint, repair semantic selectors, add fixture contract tests, and do not resume writes until the browser-write checklist passes.
