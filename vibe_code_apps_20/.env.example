# OpenFDD WattLab runtime knobs (optional — copy to .env)

# Docker image built from third_party/EnergyPlus-MCP
ENERGYPLUS_MCP_IMAGE=energyplus-mcp-dev

# Conceptual utility rates when building profile omits them
VIBE20_ELEC_RATE=0.12
VIBE20_GAS_RATE=0.80
