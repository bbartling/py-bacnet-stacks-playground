# =============================================================================
# openfdd-bacnet-feather-concept — application settings (template)
# =============================================================================
# BACnet mini-device (device 5000): AV:1–4 OA-WEATHER-*, CSV:5 location, BI:1 APP-FAULT
# Field poll targets: config/drivers/devices/*.toml (from bas_scan — not in this file)
# =============================================================================

[store]
dir = "data/feather_store"
file = "telemetry.feather"

[server]
enabled = true
instance = 5000
name = "openfdd-bacnet-feather-concept"
port = 47808
nic = "enp3s0"
address = "192.168.204.55"
broadcast = "192.168.204.255"
value_update_secs = 2
status_object_instance = 1
status_point_name = "APP-FAULT"

# Open-Meteo outdoor weather → BACnet AVs (poll every 20 min; fallbacks on failure)
[weather]
enabled = true
city = "Madison Wisconsin"
interval_secs = 1200
fallback_temp_f = 70.0
fallback_humidity = 50.0
fallback_wind_mph = 0.0
temp_object_instance = 1
temp_point_name = "OA-WEATHER-T"
humidity_object_instance = 2
humidity_point_name = "OA-WEATHER-RH"
wind_object_instance = 3
wind_point_name = "OA-WEATHER-WIND"
dewpoint_object_instance = 4
dewpoint_point_name = "OA-WEATHER-DP"
location_object_instance = 5
location_point_name = "OA-WEATHER-LOC"

[poller]
# Scheduler tick (ms) and max devices scraping in parallel
tick_ms = 250
max_concurrent = 2
# Default interval when a device omits interval_secs
interval_secs = 10
bind = "192.168.204.55"
broadcast = "192.168.204.255"

[drivers]
devices_dir = "config/drivers/devices"
