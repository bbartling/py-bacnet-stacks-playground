---
# Campus BACnet lab — run from boss Pi (bensserver):
#   cd ~/py-bacnet-stacks-playground/vibe_code_apps_14/ansible
#   ./deploy.sh
#
all:
  children:
    campus_lab:
      hosts:
        bensserver:
          ansible_host: 192.168.204.18
          campus_role: router
        building_vav:
          ansible_host: 192.168.204.14
          campus_role: vav
        # Uncomment when boss Pi can SSH to 192.168.0.13 (set deploy_ahu: true in group_vars)
        # building_ahu:
        #   ansible_host: 192.168.0.13
        #   campus_role: ahu
