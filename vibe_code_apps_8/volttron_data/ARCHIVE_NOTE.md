# Archived VOLTTRON / App8 agent material

The **`ben_bacnet/app8_web_agent`** and related **VOLTTRON** agent trees in this directory are **no longer the supported path** for BAS Lite.

Current stack: **Docker Compose** at repo **`docker-compose.yml`** + **`docker/bas_lite_api`** (easy-aso + legacy `/app8/api`) + **Caddy** + **React** build.

Keep these files for **historical reference** or migration snippets — do not expect them to match the live Docker deployment.
