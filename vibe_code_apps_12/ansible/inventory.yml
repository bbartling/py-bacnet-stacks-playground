---
# Edit ansible_user / ansible_host before running playbook.
#
#     cd vibe_code_apps_12/ansible
#     ansible-playbook deploy.yml

all:
  children:
    pi_bcn:
      hosts:
        bacnet_pi:
          ansible_host: 192.168.204.12
          ansible_user: pi
