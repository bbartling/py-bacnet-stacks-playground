---
# Example inventory — copy to inventory.yml (gitignored) and set real IPs.
#
# Public multi-building layout only. Real LAN addresses belong in inventory.yml locally.
#
#   cp inventory.example.yml inventory.yml
#   See PRIVATE-MULTI-SITE.md
#
# Deploy:
#   ./deploy.sh --limit acme_tower_a -v
#   ./deploy.sh --limit bacnet_pi -v

all:
  children:
    pi_bcn:
      hosts:
        # Synthetic lab bench (documentation IP 198.51.100.12 — replace in inventory.yml)
        bacnet_pi:
          ansible_host: 198.51.100.12
          ansible_user: ben

        # Second building — field gateway (documentation IP 198.51.100.50)
        acme_tower_a:
          ansible_host: 198.51.100.50
          ansible_user: ben

        # Acme VM test bench — Tailscale for Ansible; BACnet bind is a different LAN IP in host_vars
        acme_vm_bbartling:
          ansible_host: 198.51.100.55
          ansible_user: bbartling

        # Third site (uncomment in your private inventory.yml)
        # hospital_wing_b:
        #   ansible_host: 198.51.100.60
        #   ansible_user: ben
