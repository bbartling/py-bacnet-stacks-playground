# Operator notes (human → agent)

Append freeform notes between Codex wakes. Exported into `context_since_last_wake.md` each wake.

**Examples:**

- "Validate BRICK refs before drafting FDD rules."
- "DeployRevision bumped — re-run validate_cloud_pipeline.sh."

Do not paste passwords, PEM paths, or access keys.
