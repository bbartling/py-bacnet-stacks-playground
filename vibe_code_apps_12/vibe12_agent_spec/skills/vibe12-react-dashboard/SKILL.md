---
name: vibe12-react-dashboard
description: >-
  Vibe12 React dashboard (apps/vibe12-web). Use when updating dashboard cards,
  Arrow Rule Lab UI, data model page, or cloud pipeline explainer. Build with
  ./scripts/build_web_ui.sh before SAM deploy.
---

# Vibe12 React dashboard

- App: `apps/vibe12-web/` (Vite + React).
- Demo story: Pi/BACnet → IoT Core → DynamoDB → open-fdd Lambda → dashboard.
- Show `open_fdd_version`, `fdd_backend`, NumPy badge on slope demo rule only.
- Rule Lab title: **Arrow Rule Lab** — contract `apply_faults_arrow`.
