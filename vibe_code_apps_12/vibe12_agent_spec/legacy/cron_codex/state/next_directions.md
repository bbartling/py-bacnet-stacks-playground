# Next directions (optional long-form)

Use **`BUILD_CHECKPOINTS.md`** as the primary ordered queue — especially **"Next for mini (ordered)"** written by the last **gpt-5.5 critique**.

**Site BACnet bind, devices, cloud URLs** → **`memo../edge_backup/PHASE_NOTEPAD.md`** only.

---

## PASTE INTO NEXT WAKE (copy from here)

```
Read GUARDRAILS.md, BUILD_CHECKPOINTS.md (Last critique + Next for mini), this file,
state/context_since_last_wake.md, state/operator_notes.md, and PHASE_NOTEPAD.md.

Phase 0 lab: demo / bens-office — Pi 192.168.204.12, 6/6 telemetry, commissioning APIs.

Before claiming cloud health: ./scripts/validate_cloud_pipeline.sh
Human owns SSH and points.csv enablement.
```

---

## Standing priorities (human-editable)

1. Keep **6/6** series flowing (Pi journal + commissioning API).
2. BRICK graph + canonical model for demo building.
3. One FDD rule through Rule Lab test → go-live (human approves).
4. Multi-series dashboard (MSTP + GPIO) when P0–P2 stable.

Critique overwrites **Next for mini (ordered)** each wake; edit this file only for long-form context that does not fit checkpoints.
