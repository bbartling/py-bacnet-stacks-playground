# Quarantine — 2026-08-06

Ship artifacts moved here during hybrid Real+E+ DSM rebuild.

Do **not** restore to production stems until:

1. Real 15-min baseline + E+ paired delta models pass nested CV + recursive 96-step gates
2. Desktop hybrid walk confirms the winning model
3. Per-row manifest hash gate is wired

Honesty: prior path was absolute IdealLoads+COP kW-only screening, not hybrid.
