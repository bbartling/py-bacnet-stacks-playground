# STALE_PRE_RL

These `reports/eplus_gym` scorecards and farm-lookup overlays are **not** the current RL campaign.

Canonical live RL artifacts:

- Site raw: `SITE_ROOT/reports/eplus_gym/rl/<run_id>` (year2xsyn is frozen TRAIN)
- Git snapshot: `plots/rl_report_year2x/` (historical TRAIN, winner=null)
- Legacy unique-100: `plots/rl_report/` (LEGACY_TRAIN_EXPLORATION)

Do not treat FARM_LOOKUP_EMULATOR kW figures as current.
