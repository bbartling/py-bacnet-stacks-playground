# Browser upload widget only (YouTube / Streamlit UI). Agent/CLI/path use
# package_io DEFAULT_PACKAGE_MB=2048 — not limited by this widget.
[server]
maxUploadSize = 500

[browser]
gatherUsageStats = false
